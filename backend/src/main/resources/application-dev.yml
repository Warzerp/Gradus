# Override de configuración para entorno local de desarrollo
# Este archivo NO debe subirse a producción

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/buscador_semantico
    username: postgres
    password: jojo17935
  jpa:
    show-sql: true

app:
  jwt:
    # En desarrollo se puede usar un secret fijo (mínimo 32 caracteres = 256 bits)
    secret: dev-secret-key-do-not-use-in-production-min-256-bits
  cors:
    allowed-origins: http://localhost:5173

openai:
  api-key: ${OPENAI_API_KEY:}
